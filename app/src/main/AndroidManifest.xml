<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.betadevels.autosound"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmReceiver" android:process=":remote"/>

        <activity android:name=".AddTriggerActivity" />
        <meta-data android:name="AA_MODELS"     android:value="com.betadevels.autosound.Trigger" />
        <meta-data android:name="AA_DB_NAME"    android:value="AutoSound.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="1" />
    </application>

</manifest>
